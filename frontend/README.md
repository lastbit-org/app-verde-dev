# Frontend

Interface React do ecommerce. A página inicial é o design guide da marca.

```bash
npm install
npm run dev
```

A URL da API fica em `VITE_API_URL` (veja `.env.example`). O padrão é `http://localhost:3000`.

